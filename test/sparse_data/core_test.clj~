(ns sparse-data.core-test
  (:require [clojure.test :refer :all]
            [sparse-data.core :as sp]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))

(deftest make-spec-nested)
(deftest make-spec-flat)
(deftest save-spec)
(deftest read-spec)
(deftest make-sparse-nested)
(deftest select)
(deftest select-nil)
